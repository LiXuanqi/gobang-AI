const interval = 50;
const edgeLength = 700;
const lineWidth = 2;
const lineColor = "#777";
const chessRadius = interval / 2 - 3;

export {
    interval,
    edgeLength,
    lineWidth,
    lineColor,
    chessRadius
}